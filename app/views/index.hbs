<!DOCTYPE html>
<html lang="fr">

<head>
  <meta charset="utf-8">
  <title>{{ title }}</title>
  <link type="text/css" rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
</head>

<body>
  <div class="container">
    <h1 class="text-center text-uppercase">{{ title }}</h1>
    <div class="row">
      {{#distortions}}
      <div class="col-3">
        <div class="card" style="height: 500px">
          <img src="{{ image }}" style="height: 150px;">
          {{!-- <div class="card-header"> --}}
          {{!-- </div> --}}
          <div class="card-body text-wrap text-truncate mb-3">
            <a href="distortion/show/{{ id }}">
              <div class="btn btn-info">Voir plus -></div>
            </a>
            <div class="card-title text-uppercase font-weight-bold">{{ title }}</div>
            <p>{{ description }}</p>
          </div>
          <div class="card-footer">
            <p>Découvert par {{ finder }} en {{ release_date }}</p>
          </div>
        </div>
      </div>
      {{/distortions}}
    </div>
  </div>
</body>

</html>